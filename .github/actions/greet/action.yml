name: My Action
description: Greets user
author: Alice

inputs: 
  name:
    description: 'Кого приветствовать'
    default: 'User'
    required: true

runs:
  using: 'composite'
  steps:
    - name: 'Greet'
      shell: bash
      run: |
        echo "Hello, ${{ inputs.name }}!"